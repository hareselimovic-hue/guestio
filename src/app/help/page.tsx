import HelpClient from "./HelpClient";

export const metadata = {
  title: "Pomoć — SmartStay",
  description: "Upute za korištenje SmartStay platforme — AI asistent, sekcije, guest linkovi, analytics i Rentlio integracija.",
};

export default function HelpPage() {
  return <HelpClient />;
}
